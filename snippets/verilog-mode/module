# -*- mode: snippet -*-
# name: module
# key: module

# --
\`resetall
\`timescale 1ns / 1ps
\`default_nettype none

module `(file-name-base (buffer-file-name))` # (
    int X = 0
) (
    input wire clk,
    input wire rst
);

endmodule