# -*- mode: snippet -*-
# name: module
# key: module

# --
\`resetall
\`timescale 1ns / 1ps
\`default_nettype none

module `(file-name-base (buffer-file-name))` # (
    parameter X = 0)
(
    input clk,
    input rst
);

endmodule